{
  "name": "yookassa-codex-plugin-repository",
  "version": "0.1.0",
  "private": true,
  "description": "Codex plugin: ИИ-агент технической интеграции с ЮKassa",
  "scripts": {
    "build": "npm --prefix plugins/yookassa-integration-agent/server run build",
    "check": "npm --prefix plugins/yookassa-integration-agent/server run check",
    "test": "npm --prefix plugins/yookassa-integration-agent/server test",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "test:all": "npm run validate:plugin && npm run check && npm test"
  }
}
